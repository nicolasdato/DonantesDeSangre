<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ar.ndato.donantesdesangre.vista">
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ABMDonanteActivity"
            android:windowSoftInputMode="stateHidden" />
        <activity
            android:name=".BuscarDonanteActivity"
            android:windowSoftInputMode="stateHidden" />

        <!--
        <activity android:name=".ABMDonacionActivity"
            android:parentActivityName=".MainActivity" >
            <meta-data android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>

        <activity android:name=".EsatadisticasActivity"
            android:parentActivityName=".MainActivity" >
            <meta-data android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
        -->

        <activity android:name=".ListarDonantesActivity" />
        <activity android:name=".VerDonacionesActivity" />
        <activity
            android:name=".ABMDonacionActivity"
            android:windowSoftInputMode="stateHidden" />
        <activity android:name=".TablaCompatibilidadActivity"></activity>
    </application>

</manifest>